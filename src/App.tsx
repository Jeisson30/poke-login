import React from 'react';
import Button from '@mui/material/Button';

const App: React.FC = () => {
  return (
    <div>
      <h1>TEst</h1>
      <Button variant="contained" color="primary">
         Button MU
      </Button>
    </div>
  );
};

export default App;
